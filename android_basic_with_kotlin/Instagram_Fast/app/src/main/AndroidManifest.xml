<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hellow.instagram_fast">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MasterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Instagram_Fast"
        android:usesCleartextTraffic="true">
        <activity android:name=".OutStargramUploadActivity"></activity>
        <activity android:name=".OutStargramMyPostActivity" />
        <activity android:name=".OutStargramUserInfo" />
        <activity android:name=".OutStargramPostActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".EmailSignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>